1) What does the `rm` command in the terminal do?

Your answer:
removes the entire files

------------------
2) What terminal command would you use to list all the files and folder in the 
   current directory?

Your answer:
ls

------------------
3) Explain the difference between these two commands in the terminal:
   
   $ cd Users
   $ cd /Users

Your answer:

cd Users starts from Users path.
cd /Users starts from the root/Users path.

------------------
4) Describe what git is and what it is used for.

Your answer:
Git is a version control system, and it is used for tracking status changes of files.

------------------
5) Describe what GitHub is and what it is used for.

Your answer:
GitHub is a code hosting platform and it is used for a version control and a collaboration.

------------------
6) Which of these commands sets up a new git repository?
    a) git clone 
    b) git pull
    c) git init
    d) git status

Your answer:
c

------------------
7) You want to check that you have properly added all the files you want to be
   tracked in git BEFORE you make a commit. Which command will do this?
     a) git push
     b) git commit
     c) git status
     d) git remote

Your answer:
c

------------------
8) What is the difference between `git add` and `git commit`?

Your answer:
The 'git add' is to add files to the staging area while the 'git commit' will write changes to the repository.

------------------
9) What does the `git push` command do?

Your answer:
The 'git push' is for uploading local repository files to a remote one.

------------------
10) When would you use the `git clone` command?

Your answer:
the 'git clone' is for making a clone or copy of the existing repository.

